
import sys
import pylab as py
import numpy as np


def f(x,a):

raR = 12
  return raR**2 * np.sqrt((np.pow(vth/aw,2)) + 7.3*np.pow(10,-5) * a**2 * raR**2 * np.pow(np.sin(np.pi/2),2)) - ((1600/x)*a
  
  


